#include <stdio.h>

// Definición de la estructura para representar los datos de un estudiante
struct Estudiante {
    int matricula;
    char nombre[50];
    char carrera[50];
    float promedio;
    char direccion[100];
};
